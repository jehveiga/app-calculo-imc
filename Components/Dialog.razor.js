window.showModal = (id) => {
        let el = document.getElementById('result-dialog')
        el.showModal();
};

window.hideModal = (id) => {
        let el = document.getElementById('id')
        el.close();
};